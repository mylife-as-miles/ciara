# Moonwalk — Local macOS Backend Dependencies
# Install with: pip install -r requirements.txt
# (setup.sh handles this automatically)

# ── Core ──
google-genai>=1.0.0
python-dotenv>=1.0.0
websockets>=12.0
httpx>=0.27.0
numpy>=1.24.0

# ── Voice (macOS only) ──
pvporcupine>=3.0.0
pvrecorder>=1.2.0
SpeechRecognition>=3.10.0
requests>=2.28.0

# ── macOS Accessibility / Automation ──
pyobjc-core>=9.0
pyobjc-framework-Cocoa>=9.0
pyobjc-framework-Quartz>=9.0

# ── Voice Output (TTS) ──
google-cloud-texttospeech>=2.16.0

# ── Utilities ──
pillow>=10.0.0
tenacity>=8.0.0
