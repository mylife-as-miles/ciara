# Application Configuration
server:
  host: localhost
  port: 8080
  workers: 4

database:
  host: db.internal
  user: root
  password: secret123
  timeout: 10s
  pool_size: 5

logging:
  level: info
  format: json

debug: true
